{
  "name": "ccwatch-cli",
  "version": "0.2.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ccwatch": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile --minify src/index.ts --outfile ccwatch",
    "build:node": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.7.0"
  }
}
